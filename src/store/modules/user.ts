const state = () => ({})

const mutations = {}

const actions = {
  login ({ commit }, userInfo) {
    const { username, password } = userInfo
  }
}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
